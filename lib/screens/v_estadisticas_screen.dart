import 'package:flutter/material.dart';

class VEstadisticasScreen extends StatelessWidget {
  static const String name = 'estadisticas_screen';

  const VEstadisticasScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('VEstadisticasScreen'),
      ),
    );
  }
}
